<UserControl x:Class="Pulse.Windows.Controls.DetailCardPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="250"
             Background="Transparent">
    <Border x:Name="CardBorder" Background="{DynamicResource ThemeCardBackground}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
        <StackPanel>
            <!-- Header: Vector Logo, Provider Name & Plan -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Crisp Vector Provider Icon (1:1 with macOS LobeIconView) -->
                    <Border Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center">
                        <Path x:Name="ProviderIconPath" Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource ThemeTextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TitleText" Text="Claude Code" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource ThemeTextPrimary}"/>
                        <TextBlock x:Name="PlanText" Text="Pro Plan" FontSize="11" Foreground="{DynamicResource ThemeTextMuted}" Margin="0,1,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Border x:Name="StatusBadge" Grid.Column="1" Background="#22C55E1A" BorderBrush="#22C55E" BorderThickness="1" CornerRadius="8" Padding="7,2" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusText" Text="Live" FontSize="10" FontWeight="Medium" Foreground="#4ADE80"/>
                </Border>
            </Grid>

            <Separator Background="{DynamicResource ThemeBorderBrush}" Margin="0,2,0,8"/>

            <!-- Windows List -->
            <StackPanel x:Name="WindowsContainer">
                <!-- Dynamically populated rows -->
            </StackPanel>

            <!-- Credit Balance Section if available -->
            <Grid x:Name="CreditSection" Visibility="Collapsed" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Balance" FontSize="11" Foreground="{DynamicResource ThemeTextMuted}"/>
                <TextBlock x:Name="CreditText" Grid.Column="1" Text="$0.00" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource ThemeTextPrimary}"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
